#include <stdio.h>
/**
 * main - main to run other functs
 * @argc: the counting for ....
 * @argv: the vector
 * Return: 0 on successfull
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
